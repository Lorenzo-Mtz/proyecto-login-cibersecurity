# Registro de Riesgos / Risk Register

**Proyecto:** Sistema de Login Seguro — Proyecto de Aprendizaje en Ciberseguridad
**Fecha de elaboración:** Agosto 2026
**Última revisión:** Septiembre 2026 — **threat model de la Fase 3** (WBS 5.2.5): se agregan R17 y R18, y se releé R12, cuya aceptación resultó estar mal delimitada. Antes: apertura de la Fase 3 (WBS 5.1.3), que agregó R15 y R16 y actualizó R1 y R5. Antes: cierre de la Fase 2 (WBS 4.6.3): revisión completa de R1 – R14, R2 actualizado con el dato real de duración. Antes: cierre de WBS 4.5.6 (R5 actualizado con los huecos que destapó el recorrido del OWASP Top 10). Antes: cierre de WBS 4.5.5 (R6 marcado como Mitigado). Antes: cierre de WBS 4.5.4 (riesgo residual de R9 cerrado, y su enunciado **corregido**: estaba de menos). Antes: cierre de WBS 4.3 (R4 marcado como Mitigado; R12 y R13 confirmados como aceptados)
**Preparado por:** Lorenzo Mtz

---

## 1. Propósito

Este registro identifica los riesgos (amenazas y oportunidades) que pueden afectar el proyecto, con una respuesta planeada para cada uno. Al ser un proyecto individual, se usa una versión **tailored** (adaptada) del Risk Register de PMBOK: sin gestión cuantitativa de riesgo, solo evaluación cualitativa simple.

Es un documento **vivo**: se revisa al cierre de cada fase (ver WBS, tareas "Cierre de Fase"), igual que el Lessons Learned Log.

---

## 2. Escala de Probabilidad e Impacto

| Nivel | Probabilidad | Impacto |
|---|---|---|
| **Baja** | Poco probable que ocurra | Efecto menor, fácil de absorber |
| **Media** | Podría ocurrir | Retrasa una fase o requiere ajustar el plan |
| **Alta** | Muy probable / ya está ocurriendo | Pone en riesgo un objetivo del Charter |

**Exposición** = combinación cualitativa de Probabilidad × Impacto (Baja / Media / Alta).

---

## 3. Registro de Riesgos (Amenazas)

| ID | Categoría | Descripción del riesgo | Probabilidad | Impacto | Exposición | Estrategia | Plan de respuesta |
|---|---|---|---|---|---|---|---|
| R1 | Alcance | El criterio original "calidad de mercado" era ambiguo y puede generar scope creep | Media | Media | Media | Mitigar | Ya mitigado en el Charter: criterios de éxito objetivos = OWASP ASVS Level 1 (Sección 3). **Actualización (apertura de Fase 3):** el mismo riesgo reapareció al planear la fase — encontrar gaps invita a arreglarlos. Se acotó por decisión registrada (Charter, cambio #5): la Fase 3 **documenta y no corrige**, y la remediación se propone como fase aparte en 5.4.3 |
| R2 | Cronograma | Subestimar el tiempo real de la Fase 2 (MFA, rate limiting, logging) por ser la más técnica | Alta | Media | Alta | Mitigar | Reservar margen extra al planear Fase 2; desglosar a work packages (WBS) *antes* de empezar a programarla, no a mitad de camino. **Actualización (Septiembre 2026, inicio de Fase 2):** desglose hecho antes de programar (WBS v1.2, paquetes 4.1 – 4.6) con orden de ejecución por dependencias; se implementa, prueba y commitea un paquete a la vez, y MFA (4.3), el más complejo, va al final para no bloquear el resto. Referencia: la Fase 1 tomó ~2 semanas para 12 paquetes de trabajo (3.1 – 3.5); la Fase 2 tiene 28 (4.1 – 4.5) y más complejos, así que no se fija fecha de cierre. **Actualización (Septiembre 2026, cierre de Fase 2):** la fase se completó del 14 al 22 de septiembre de 2026, unos nueve días para los seis paquetes. El riesgo **no se materializó**, y la razón identificable no es que se estimara mejor —no se estimó— sino el desglose previo a programar: un paquete a la vez, cada uno con su commit y su verificación, sin fecha comprometida. Se mantiene **En seguimiento** para la Fase 3, que vuelve a ser terreno nuevo (autoevaluación ASVS y threat model) y donde la misma trampa aplica |
| R3 | Recursos | Disponibilidad de tiempo variable (4-8 hrs/semana) alarga los tiempos entre hitos | Alta | Baja | Media | Aceptar | Se acepta como restricción natural del proyecto (ver Charter, Sección 8); se compensa con rolling wave planning en vez de fechas fijas |
| R4 | Técnico | Primera implementación de MFA/TOTP: alto riesgo de implementarlo de forma insegura o incorrecta sin experiencia previa | Media | Alta | Alta | Mitigar | Reforzar el tema en Fase 0 (notas de estudio 0.5) antes de programarlo; usar una librería TOTP probada (ej. `pyotp`) en vez de implementar el algoritmo desde cero. **Actualización (Septiembre 2026, inicio de Fase 2):** los conceptos quedaron cubiertos en Fase 0 (glosario: OTP, HOTP, TOTP, secreto compartido, QR de aprovisionamiento) y se confirma `pyotp`. Los errores típicos se convirtieron en paquetes explícitos del WBS 4.3: activar MFA solo tras validar un primer código, sesión "pendiente de MFA" sin acceso a rutas protegidas, tolerancia de ±1 periodo, rechazo de códigos reutilizados y límite de intentos. **Estado: Mitigado (Septiembre 2026)** — los cinco controles están implementados en `src/mfa.py` y `src/routes/auth.py`, con el algoritmo en `pyotp` (RFC 6238) y no en código propio. `verificar_codigo()` devuelve el **número de periodo aceptado** y no un booleano, que es lo que hace posible el control de reutilización: se exige que el periodo nuevo sea estrictamente mayor que el guardado, lo que rechaza el código repetido **y** los anteriores de la ventana. La comparación usa `hmac.compare_digest` y va precedida de un filtro de formato, porque un código no ASCII lanzaría `TypeError` y terminaría en un 500, que es una respuesta distinta a "código incorrecto". Dos controles que no estaban en el plan y cerraron agujeros reales: la sesión pendiente **caduca a los 5 minutos** (es una contraseña ya validada esperando el segundo factor) y la contraseña correcta **no reinicia el contador de 4.1** cuando hay MFA, porque si lo hiciera se podrían probar códigos de cinco en cinco volviendo a loguearse. **Verificado** con 28 pruebas automatizadas, **16 mutaciones** (cada control roto a propósito para confirmar que una prueba se pone en rojo) y la prueba manual de 4.3.6 con **Google Authenticator** sobre un teléfono real: el enrolamiento activó MFA, el login en dos pasos funcionó, y **un código reutilizado fue rechazado** (`mfa_failure` a las 02:32:00, `login_success` con un código nuevo a las 02:32:06). **Hueco conocido:** sustituir `compare_digest` por `==` no lo atrapa ninguna prueba —medido, no supuesto—; ese control se sostiene por revisión de código |
| R5 | Seguridad | Al ser el primer proyecto práctico de seguridad, se pueden introducir vulnerabilidades sin darse cuenta (falsa sensación de seguridad) | Media | Alta | Alta | Mitigar | Autoevaluación honesta contra checklist ASVS Level 1 (Fase 3) antes de dar el proyecto por "seguro"; no asumir que "funciona" = "seguro". **Nota (apertura de Fase 3):** esta fase es la respuesta planeada a este riesgo, y por eso se abrió **R15** — el riesgo de que la autoevaluación misma falle por complacencia. **Actualización (Septiembre 2026, WBS 4.5.6):** el recorrido del OWASP Top 10:2025 (`docs/fase2-owasp-top10.md`) es el primer paso de esa autoevaluación y ya cumplió su función de romper la falsa sensación de seguridad: además de confirmar los residuales ya registrados, **destapó cinco huecos que no estaban en ningún artefacto** — ausencia total de cabeceras de seguridad, ausencia de *alerting*, rotación e integridad del log, falta de manejadores globales de error (un 500 imprevisto ni se maneja ni se audita), falta de verificación por hash de las dependencias, y que `@login_required` no es *deny by default*, así que una ruta nueva sin el decorador queda abierta y nada avisa. Todos anotados en la deuda de Fase 2. **Estado: En seguimiento** — se cierra con la autoevaluación ASVS de Fase 3 |
| R6 | Técnico / Cadena de suministro | Dependencias (librerías de Python) con vulnerabilidades conocidas | Baja | Media | Baja | Mitigar | Revisar dependencias con `pip list` / herramientas como `pip-audit` antes del cierre de cada fase. **Estado: Mitigado (Septiembre 2026, WBS 4.5.5)** — auditoría con `pip-audit 2.10.1` contra la PyPI Advisory Database: **0 vulnerabilidades conocidas** sobre los **22 paquetes** del entorno, más `pip` 26.2.1 auditado aparte porque `pip freeze` lo omite. Lo importante es **qué** se auditó: `requirements.txt` declara 8 paquetes y el entorno tiene 22; los 14 restantes son transitivos (Werkzeug, Jinja2, itsdangerous, MarkupSafe, click y blinker de Flask; dnspython e idna de email-validator; colorama de qrcode; pluggy, iniconfig, packaging y Pygments de pytest). Auditar solo el archivo declarado habría sido auditar la lista corta y no la superficie real: en una app Flask los avisos históricamente caen en **Werkzeug y Jinja2**, no en Flask. El auditor se corrió desde un **venv desechable** contra un `pip freeze` del entorno, y no instalado dentro de él: `pip-audit` arrastra su propia cadena (~10 paquetes) que, instalada en `.venv`, pasaría a formar parte de lo auditado y mezclaría los hallazgos del auditor con los del proyecto. En el mismo paquete se separó `requirements-dev.txt` de `requirements.txt`, para que un despliegue no instale herramientas de desarrollo. **Limitación del control:** `pip-audit` compara nombre + versión contra avisos **publicados**; no analiza el código ni cómo se usa cada librería, así que un resultado limpio significa "nada conocido hoy", no "sin vulnerabilidades". Por eso el plan es re-correrlo en cada cierre de fase y no una sola vez |
| R7 | Seguridad / Operacional | Exponer credenciales o secretos por accidente en el historial de Git | Baja | Alta | Media | Mitigar | `.gitignore` ya cubre `.env`, `*.key`, `*.pem`, `*.db` (ver guía de Git, Sección 6); revisar `git status` antes de cada `add` |
| R8 | Gobernanza del proyecto | Los artefactos PMBOK (Charter, Scope, WBS) se desactualizan y dejan de reflejar el alcance real | Media | Baja | Baja | Mitigar | Usar el Registro de Cambios del Charter (Sección 10) cada vez que el alcance se ajuste, como ya se hizo al agregar la Fase 0 |
| R9 | Seguridad | Flask guarda la sesión en una cookie firmada del lado del cliente: `logout()` no la invalida en el servidor, y una cookie robada sigue siendo válida mientras se use (`PERMANENT_SESSION_LIFETIME` **no** es una vida máxima: ver la corrección de septiembre de 2026 en el plan de respuesta) | Baja | Alta | Media | Mitigar | Columna `session_version` en `users`: se guarda en la sesión al hacer login, se valida en cada ruta protegida y se incrementa en logout (y en cambio de contraseña, Fase 2). **Estado: Mitigado (Septiembre 2026)** — implementado en `schema.sql` y `auth.py`; verificado que una cookie copiada antes del logout es rechazada. Riesgo residual: si el usuario nunca hace logout, una cookie robada sigue válida hasta 14 días (se atiende en WBS 4.5.4). **Corrección (Septiembre 2026, WBS 4.5.4):** ese riesgo residual estaba mal enunciado, y de menos. `PERMANENT_SESSION_LIFETIME` no es una vida máxima sino un tiempo **sin peticiones**: Flask lo valida contra el timestamp que va dentro de la firma (`open_session` → `max_age`), así que se hace cumplir del lado del servidor y no depende del `Expires` que el navegador puede ignorar —eso está bien—, pero `SESSION_REFRESH_EACH_REQUEST` viene en `True` por default y vuelve a firmar la cookie en **cada petición**, de modo que la ventana **se desliza**. Una cookie robada que se usa no caducaba a los 14 días: no caducaba nunca, porque el atacante la renovaba al usarla. **Estado del residual: Cerrado (Septiembre 2026)** — con dos controles que se complementan y que es fácil confundir. **(a) Expiración por inactividad:** `PERMANENT_SESSION_LIFETIME` bajó a **60 minutos**, que es lo que esa clave sí ofrece. **(b) Vida máxima:** `SESSION_ABSOLUTE_LIFETIME_SECONDS` = **12 horas**, contadas desde el login y que la actividad **no** renueva; se comprueba en `@login_required` contra `session["login_at"]` **antes** del `SELECT`, para que una cookie vencida no cueste una consulta. Es el único de los dos que una cookie robada no puede estirar usándola, y por eso es el que cierra este riesgo residual; (a) por sí solo no lo habría cerrado. La marca va en la cookie firmada y no en la BD —es estado de la conversación, no de la cuenta, y la firma impide retrasarla—, y su **ausencia** se trata como sesión vencida (*fail-closed*), lo que cubre tanto las cookies emitidas antes de este cambio como cualquier punto futuro que abra sesión y olvide ponerla. El rechazo se audita como `session_expired`, evento propio y no `session_rejected`, porque caducar y ser revocada son hechos distintos y mezclarlos pierde señal al leer el log (LL19). Verificado con **6 pruebas** y **4 mutaciones**, todas atrapadas. **Limitación conocida:** la expiración por inactividad **no puede auditarse**. Flask descarta la cookie vencida en `open_session` y la sesión llega vacía a la vista, indistinguible de un visitante sin sesión: no hay `user_id` a quien atribuir el evento. Queda fijado con una prueba, para que salte si algún día cambia |
| R10 | Seguridad | El bloqueo temporal por intentos fallidos (4.1) puede usarse para bloquear a propósito a un usuario legítimo (DoS de cuenta), o revelar qué cuentas existen si el mensaje o el tiempo de respuesta cambian al bloquear | Media | Media | Media | Mitigar | Bloqueo temporal corto, nunca permanente; contador por nombre de usuario enviado, exista o no la cuenta; mismo mensaje genérico y misma comparación contra `DUMMY_HASH` que un login fallido normal. **Estado: Mitigado (Septiembre 2026)** — `src/throttle.py` y el guard de `login()`: 5 fallos en 15 minutos, con ventana deslizante que libera sola (nunca permanente); la tabla `login_attempts` no tiene `FOREIGN KEY` a `users`, así que las cuentas inexistentes se cuentan igual; el camino bloqueado ejecuta `bcrypt.checkpw` contra `DUMMY_HASH` y **no registra el intento**, de modo que un atacante no puede prolongar el bloqueo de una víctima. Verificado con 13 pruebas sobre el flujo HTTP: la contraseña correcta es rechazada durante el bloqueo, `MAX(id)` no cambia tras tres intentos bloqueados, la latencia bloqueado/fallo normal fue 371 ms vs 340 ms (ratio 1.09) y el bloqueo se liberó solo al vencer la ventana. **Riesgo residual:** contar por nombre de usuario no detecta *password spraying* (una contraseña común contra muchas cuentas), que requiere límite por IP, fuera del alcance de 4.1 |
| R11 | Seguridad | El token de recuperación de contraseña (4.2) es predecible, reutilizable, no expira, o el formulario revela si un email está registrado | Media | Alta | Alta | Mitigar | Token con `secrets.token_urlsafe`; en la BD solo su hash SHA-256; expiración corta; marcado como usado al consumirse; mensaje genérico en la solicitud; al cambiar la contraseña se incrementa `session_version`. **Estado: Mitigado (Septiembre 2026)** — `src/reset_tokens.py`, `src/mailer.py` y las tres rutas de `auth.py`: token de `secrets.token_urlsafe(32)` (256 bits de un CSPRNG, no predecible), del que la BD guarda **solo el SHA-256**; vigencia de 30 minutos calculada por SQLite; un token nuevo borra los anteriores del mismo usuario; inexistente, expirado y ya usado se rechazan por el mismo camino y con el mismo mensaje; el `flash` y el `redirect` de `/forgot-password` están fuera del `if`, así que la respuesta es idéntica exista o no el email; el enlace no sale por la respuesta HTTP ni por `audit.log` (solo por consola), y el formulario del token vive en una ruta propia que **no audita**, porque en el GET el token viaja en `request.path`. Al consumirse, la contraseña y `session_version + 1` se escriben en la misma sentencia (cierra las sesiones abiertas, R9) y se reinicia el contador de 4.1. Verificado con 14 pruebas sobre el flujo HTTP y, además, rompiendo cada control a propósito (diez mutaciones) para confirmar que la prueba correspondiente se pone en rojo. **Riesgos residuales:** (a) `/forgot-password` no tiene límite de tasa, y como un token nuevo invalida los anteriores, quien conozca un email puede pedir tokens en serie y mantener invalidado el que la víctima está por usar (DoS de recuperación); con SMTP real sería además un vector de envío masivo; (b) el token viaja en la URL, así que queda en el historial del navegador y podría filtrarse por `Referer` |
| R12 | Seguridad | El secreto TOTP (4.3) debe guardarse de forma recuperable para validar códigos: quien lea la BD SQLite puede generar códigos MFA válidos | Baja | Alta | Media | Aceptar | Cifrarlo requiere gestión de llaves, fuera del alcance de esta iteración. La BD ya está fuera del repo (`*.db` en `.gitignore`, R7). Se documenta como gap en la autoevaluación ASVS (Fase 3). **Actualización (Septiembre 2026, cierre de 4.3):** implementado tal como se aceptó — `users.totp_secret` guarda el secreto en claro, porque validar un código TOTP exige **recalcular** un HMAC y no se puede calcular un HMAC con el hash de la llave. Se acotó la exposición: el secreto **no** entra a `g.user` (el `SELECT` de `@login_required` lista columnas explícitas y no `*`, justamente para que no quede al alcance de cualquier plantilla en cada petición), **no** se guarda en la cookie de sesión —que va firmada pero no cifrada— y **no** llega a `audit.log`. El QR se embebe en la página como `data:` URI y no se sirve desde una ruta propia, que sería una URL entregando el secreto compartido. **Relectura (Septiembre 2026, threat model de Fase 3):** la aceptación **sigue siendo válida** —cifrar el secreto exige gestión de llaves, que sigue fuera del alcance— pero estaba **mal delimitada**. Al modelar el perfil de atacante sobre el que se aceptó, resultó que ese mismo atacante alcanza la `SECRET_KEY`, la escritura de la base y el registro de auditoría, todo ello de mayor impacto que el secreto TOTP. Redactada así, esta entrada daba a entender que la exposición estaba acotada al segundo factor. **El alcance real de ese perfil se registra ahora en R17.** **Estado: Aceptado** |
| R13 | Técnico | Perder el dispositivo con la app autenticadora deja la cuenta inaccesible: los códigos de respaldo (*backup codes*) no están en el alcance | Baja | Media | Baja | Aceptar | Proyecto individual sin usuarios reales: la recuperación es manual, desactivando MFA directamente en la BD. Los códigos de respaldo quedan como mejora futura, vía Registro de Cambios del Charter. **Actualización (Septiembre 2026, cierre de 4.3):** este riesgo decidió el diseño del enrolamiento. MFA quedó **opcional y activable desde el dashboard**, no obligatorio durante el registro: con recuperación manual, hacerlo obligatorio dejaría cada cuenta nueva a un teléfono perdido de distancia de necesitar acceso a SQLite. Por lo mismo, MFA solo se activa **después** de validar un primer código, de modo que abrir la página del QR y cerrarla no deja a nadie fuera de su cuenta. **Estado: Aceptado** |
| R14 | Seguridad | El registro de auditoría (4.4) filtra datos sensibles (contraseñas, tokens, códigos TOTP escritos por accidente) o permite *log injection* mediante saltos de línea en el nombre de usuario | Media | Alta | Alta | Mitigar | Catálogo cerrado de eventos y campos (4.4.1); regla explícita de no registrar secretos y neutralizar saltos de línea (4.4.3); archivo de log ignorado por git (4.4.2). **Estado: Mitigado (Septiembre 2026)** — `src/audit.py`: la firma de `audit()` no acepta campos libres, así que un secreto no puede pasarse por accidente; cada evento se escribe como una línea JSON (`json.dumps` escapa saltos de línea y comillas) y el `username` se recorta a 64 caracteres. Verificado: un usuario con un salto de línea y un `login_success` falso quedó registrado en una sola línea, escapado, como `login_failure`. **Riesgo residual:** si alguien escribe su contraseña en el campo "Usuario" por error, queda en el log como `username` de un `login_failure` |
| R15 | Seguridad / Gobernanza | La autoevaluación ASVS (Fase 3) la hace quien escribió el código: el sesgo natural empuja a marcar `pass` sin evidencia o `N/A` sin argumento, y el resultado sería un documento que **certifica lo contrario de la realidad** — peor que no tenerlo, porque da por auditado lo que no se miró | Media | Alta | Alta | Mitigar | Tres reglas fijadas antes de empezar: (a) **evidencia obligatoria** por control —archivo, o la prueba automatizada que lo sostiene—, nunca una afirmación suelta; (b) **tres estatus y no dos**, separando *no aplica a esta arquitectura* de *fuera del alcance del proyecto*, porque lo segundo es un gap real y no una exención (el caso claro es TLS: no es inaplicable, es que no hay despliegue); (c) los `fail` **se documentan y no se corrigen** dentro de la fase, para que arreglar no compita con reportar ni tiente a suavizar el hallazgo. Es la continuación directa de R5 |
| R16 | Cronograma / Alcance | ASVS 5.0 abarca 17 capítulos y el volumen real de requisitos de nivel 1 aplicables **no se conoce** hasta filtrar el estándar. Puede exceder la disponibilidad de 4–8 h/semana y dejar un checklist parcial presentado como completo | Media | Media | Media | Mitigar | No planear el detalle antes de tener el número: 5.3.1 (obtener el CSV oficial) y 5.3.2 (triaje por capítulo) van primero, y 5.3.3 es un **punto de decisión explícito** en el WBS para dimensionar y acordar el nivel de detalle. Un commit por bloque de capítulos, como funcionó en la Fase 2. Si el número no cabe, la salida es **acotar el alcance por cambio formal en el Charter** y decirlo en el propio documento, no recortar en silencio |
| R17 | Seguridad | **El perfil de atacante con acceso al sistema de archivos rompe el sistema entero, no solo el segundo factor.** R12 aceptó ese perfil razonando únicamente sobre `users.totp_secret`, pero el mismo atacante lee `.env` y con la **`SECRET_KEY`** fabrica una sesión de cualquier usuario —ningún control de sesión sobrevive: ni `session_version`, ni los timeouts, ni MFA—, escribe en `users` para poner `mfa_enabled = 0`, borra `audit.log` para ocultar su rastro, y elimina `app.db` sin que haya respaldos | Baja | Alta | Media | Mitigar | Identificado en el threat model de Fase 3 (`docs/fase3-threat-model.md`, sección 7; amenazas TM-24, TM-25, TM-26 y TM-29). **El riesgo no estaba mal analizado, estaba mal delimitado:** la aceptación de R12 se hizo sobre el activo *menos* valioso de los que ese atacante alcanza. Mitigaciones candidatas, por orden de valor y fuera del alcance de la Fase 3, que documenta y no corrige: permisos restrictivos sobre `instance/` y `.env`; rotación de la `SECRET_KEY` documentada como procedimiento; copias de respaldo de la base; log *append-only*. Ninguna elimina el riesgo —quien controla el host controla la aplicación— pero hoy no hay **ninguna**, que es lo que el registro no reflejaba. **Estado: Abierto** |
| R18 | Seguridad / Gobernanza | **El registro de auditoría no sostiene no repudio.** El archivo no es *append-only* ni está firmado, así que quien pueda escribir en `instance/` edita o borra líneas; además hay huecos de trazabilidad deliberados o no cubiertos —el GET de `/reset-password/<token>` no audita para no escribir el token, y un 500 imprevisto tampoco— y el archivo crece sin rotación, de modo que llenar el disco es una denegación de servicio | Media | Media | Media | Mitigar | Identificado en el threat model de Fase 3 (amenazas TM-03, TM-11, TM-26 y TM-28). El peligro no es que falte el log —existe, con catálogo cerrado de 15 eventos y pruebas propias— sino que **da una garantía que no tiene**: se confía en él como evidencia y no puede serlo frente al perfil de R17. Mitigaciones candidatas: rotación con techo de tamaño; envío a un destino fuera del alcance de escritura de la aplicación; registro de los 500 sin filtrar detalle; y algo que **lea** el archivo, que hoy no existe (A09 del recorrido del Top 10). **Estado: Abierto** |

---

## 4. Registro de Riesgos (Oportunidades)

PMBOK también contempla riesgos positivos — cosas que, si ocurren, benefician al proyecto más allá de lo planeado.

| ID | Categoría | Descripción de la oportunidad | Probabilidad | Impacto | Estrategia | Plan de respuesta |
|---|---|---|---|---|---|---|
| O1 | Portafolio profesional | La documentación PMBOK + el proyecto técnico completo pueden servir como pieza de portafolio para prácticas profesionales o empleo | Media | Alta | Mejorar (Enhance) | Mantener el repo con buena calidad de documentación desde ahora, pensando en que alguien más lo pueda leer eventualmente |

---

## 5. Notas de seguimiento

- Este registro se revisa al cierre de cada fase (Fase 0, 1, 2, 3) — se agregan riesgos nuevos que hayan surgido y se cierran los que ya no aplican.
- Los riesgos cerrados no se borran: se marcan como **Cerrado** para mantener trazabilidad, siguiendo el mismo principio de control de cambios usado en el Charter.
