# Lessons Learned Log

**Proyecto:** Sistema de Login Seguro — Proyecto de Aprendizaje en Ciberseguridad
**Preparado por:** Lorenzo Mtz

---

## Propósito

Registro continuo de lecciones aprendidas — técnicas, de herramientas y de gestión de proyecto. No se espera al cierre del proyecto para llenarlo: se agrega una entrada cada vez que algo enseña algo útil, sea un error o un acierto. Se revisa formalmente al cierre de cada fase (ver WBS).

---

## Registro

| ID | Fecha | Fase | Categoría | ¿Qué pasó? | Lección aprendida | Acción a futuro |
|---|---|---|---|---|---|---|
| LL1 | Agosto 2026 | M0 (Setup) | Herramientas (Git) | Se ejecutó `git commit` sin haber corrido `git add` antes; el commit no guardó nada | `git commit` solo confirma lo que ya está en el área de preparación (*staging area*) — no detecta cambios por sí solo | Correr `git status` como paso previo obligatorio antes de cualquier `commit` |
| LL2 | Agosto 2026 | M0 (Setup) | Convenciones / Nomenclatura | Archivos creados con nombres inconsistentes (`Wbs.md` con mayúscula, acento accidental en `scopé-statement.md`), detectado ya después de subirlos a GitHub | Windows no distingue mayúsculas/minúsculas en nombres de archivo, pero Git sí las rastrea tal como se guardan; los acentos automáticos del sistema pueden colarse sin notarlo | Revisar el nombre exacto del archivo (con `ls`) antes de hacer el primer `git add` |
| LL3 | Agosto 2026 | M0 (Setup) | Git | Fue necesario corregir nombres de archivos ya trackeados por Git | `mv` normal hace que Git vea un archivo borrado + uno nuevo, perdiendo el historial de ese archivo; `git mv` sí registra el cambio como un rename | Usar siempre `git mv` para renombrar archivos que ya están trackeados, nunca `mv` a secas |
| LL4 | Septiembre 2026 | Fase 1 | Técnico (SQLite) | Se agregó la columna `session_version` a `schema.sql`, pero la BD existente no la tenía y las consultas fallaban | `CREATE TABLE IF NOT EXISTS` solo crea tablas nuevas: si la tabla ya existe no hace nada, así que no migra columnas. Además, SQLite parsea toda la sentencia antes de evaluar el `IF NOT EXISTS`, por lo que un error de sintaxis (una coma faltante) rompe el arranque aunque la tabla exista | Al cambiar el esquema, recrear la BD de desarrollo o aplicar un `ALTER TABLE`; en fases futuras considerar scripts de migración |
| LL5 | Septiembre 2026 | Fase 1 | Seguridad (Sesiones) | Se asumió que `session.clear()` en el logout invalidaba la sesión, pero una cookie copiada antes del logout seguía siendo válida | Flask guarda la sesión en una cookie firmada del lado del cliente, no en el servidor: firmar evita que se modifique, pero no permite revocarla. Invalidar requiere estado en el servidor (se implementó `session_version`, ver R9) | Ante cualquier mecanismo de sesión, preguntar "¿dónde vive el estado?" y probar explícitamente el escenario de cookie robada, no solo el flujo feliz |
| LL6 | Septiembre 2026 | Fase 1 | Técnico (Flask) | Se escribió `session.permanent()` y el login tronaba con `TypeError: 'bool' object is not callable` | `session.permanent` es un atributo booleano que se asigna (`= True`), no un método. Además, `PERMANENT_SESSION_LIFETIME` no tiene efecto si la sesión no se marca como permanente | Revisar la documentación oficial antes de usar una API nueva, y verificar el resultado real (fecha *Expires* de la cookie en DevTools) en vez de asumir que la configuración basta |
| LL7 | Septiembre 2026 | Fase 1 | Herramientas (Git) | `git push` fue rechazado porque GitHub tenía un commit hecho desde la web que no estaba en la copia local | Editar desde GitHub y localmente a la vez divide la historia; Git rechaza el push para no sobrescribir trabajo remoto. Se resolvió con `git fetch` + `git rebase origin/main` (sin `--force`) | Correr `git pull --rebase` antes de empezar a trabajar localmente, sobre todo si se editó algo desde la web |
| LL8 | Septiembre 2026 | Fase 2 | Seguridad (Configuración) | Al hacer obligatoria la `SECRET_KEY` (WBS 4.5.1) se descubrió que durante toda la Fase 1 la app firmó las cookies con el valor por defecto `"dev-only-inseguro-cambiame"`. `.env` nunca se creó, pero aunque hubiera existido no se habría leído: `app.py` importaba `Config` antes de llamar a `load_dotenv()` | Los atributos de una clase se evalúan al importar el módulo, no al usarla. Un valor por defecto "de desarrollo" oculta el error: la app funciona igual y nada avisa que el secreto es público. Por eso fallar al arrancar es más seguro que tener un *fallback* | Para secretos, sin valores por defecto: validar al arrancar y detener la app si faltan. Verificar el valor efectivo en ejecución (`app.config`), no solo que el archivo de configuración exista |
| LL9 | Septiembre 2026 | Fase 2 | Técnico (Python `logging`) | Al implementar el registro de auditoría (WBS 4.4), `audit_logger.info(...)` no escribía nada: no se creaba el archivo, no había error ni excepción | Un logger sin `setLevel` hereda `WARNING` del logger raíz y **descarta los mensajes INFO en silencio**. Además, `logging` son tres piezas que hay que conectar a mano: logger → handler → formatter; crear un objeto no lo conecta con nada | Cuando un log no aparece, revisar primero el nivel efectivo (`getEffectiveLevel()`) y que el handler esté realmente agregado. En general, desconfiar de los mecanismos que fallan sin hacer ruido |
| LL10 | Septiembre 2026 | Fase 2 | Pruebas (Diseño) | Al probar el bloqueo por fuerza bruta (WBS 4.1.5) se puso la ventana en 3 segundos para no esperar. Cada request tarda ~330 ms por bcrypt, así que la ventana venció **a media prueba**: un intento que debía contarse como bloqueado se registró como fallo normal. La comprobación de "el contador no crece" siguió dando verde, porque `record_failed_attempt` insertó una fila y su limpieza borró una vieja, dejando el total igual | Una prueba cuyo parámetro temporal es más corto que el costo de la operación que mide termina midiendo otra cosa. Y peor: una aserción sobre un agregado (`COUNT(*)`) puede pasar por compensación de dos errores. El `COUNT` se cambió por `MAX(id)`, que distingue "no se insertó nada" de "se insertó y se borró otra" | Al acortar tiempos para hacer una prueba viable, verificar que el margen siga siendo holgado frente a la latencia real del sistema. Preferir aserciones sobre hechos concretos (¿se insertó esta fila?) antes que sobre totales, que varias fallas pueden dejar intactos |
| LL11 | Septiembre 2026 | Fase 2 | Técnico (Python) | Al extraer la política de contraseñas a `validar_password()` para compartirla entre `/register` y el reset, la función devolvía `False` en cada rama de error pero **no devolvía nada** en el camino bueno. `not None` es verdadero, así que toda contraseña válida entraba por la rama de error | Una función que devuelve un booleano tiene que retornarlo **en todos sus caminos**. Python no avisa: un `return` faltante devuelve `None` en silencio, y `None` se evalúa como falso. En un validador eso invierte el resultado del control completo | Al escribir una función que decide algo, revisar que cada camino de salida tenga un `return` explícito. Desconfiar en particular del camino feliz, que suele ser el último que se escribe y el único sin `return` propio |
| LL12 | Septiembre 2026 | Fase 2 | Seguridad (Diseño de controles) | `register()` llamaba a la validación con `if not validar_password(...): redirect(...)`, **sin `return`**. Combinado con LL11 el efecto fue el peor posible: la política de contraseñas dejó de existir y la cuenta `abc` se registró sin que nada fallara ni apareciera un error | `redirect()` solo construye una respuesta; sin `return` no detiene nada y la función sigue ejecutándose. La lección real es la asimetría: un control que **falla cerrado** (rechaza de más) se reporta en cinco minutos, y uno que **falla abierto** (deja pasar) no se nota nunca, porque la aplicación se ve idéntica. Refactorizar un control de seguridad sin una prueba que lo sostenga es mover un muro de carga a ojo | Ningún control de seguridad se refactoriza sin una prueba que lo cubra **antes** de tocarlo. Al revisar un guard, preguntar no si rechaza lo malo, sino qué pasa si deja de rechazarlo: si la respuesta es "nada visible", hace falta una prueba |
| LL13 | Septiembre 2026 | Fase 2 | Técnico (Python) | Se usó `id` como nombre de variable para el identificador de una fila; la consulta a SQLite falló con un error sobre un tipo no soportado en lugar de un `NameError` | `id` es una función *builtin* de Python: usarla como variable no produce ningún error, solo la sombra. Si el valor nunca se asigna en esa rama, lo que se termina pasando a SQLite es la función misma. El error aparece lejos del lugar donde está la causa | No usar nombres de builtins (`id`, `type`, `list`, `hash`, `input`) como variables. Ante un error de tipo inexplicable en una consulta, revisar primero que el parámetro sea el valor que se cree que es |
| LL14 | Septiembre 2026 | Fase 2 | Pruebas (Diseño) | La prueba de que el token correcto se marca como usado pasaba en verde con el bug puesto. Con un solo usuario, `user_id` y `token_id` valen ambos 1, así que confundir una columna con la otra marca la fila correcta por casualidad. Se reescribió con dos usuarios… **y siguió pasando**: el orden en que se emitían los tokens volvía a alinear los ids (usuario 2 con token 2). Solo se detectó al romper el código a propósito y ver que la prueba no se ponía en rojo | Una prueba que nunca se ha visto fallar no prueba nada. En datos de prueba pequeños los identificadores coinciden por accidente y esconden justo los errores de "columna equivocada"; agregar una segunda fila no basta si el orden vuelve a alinearlos. La única verificación real de una prueba es romper el control que dice cubrir y confirmar que falla (*mutation testing*) | Al cerrar un paquete, romper cada control a propósito —una mutación a la vez, revirtiendo después— y confirmar que la prueba correspondiente se pone en rojo. En los datos de prueba, desalinear los ids de forma explícita y dejar un `assert` que lo verifique, para que nadie los realinee sin darse cuenta |
| LL15 | Septiembre 2026 | Fase 2 | Seguridad (Fugas de información) | En el formulario "olvidé mi contraseña", el mensaje era genérico exista o no el email, pero el código desempacaba el resultado de un `fetchone()` que devolvía `None` cuando el email no existía. El resultado: **302 si el email estaba registrado, 500 si no** | Que dos ramas *digan* lo mismo no sirve si no *terminan* igual. Un error 500 es tan observable como un mensaje distinto, y construye un oráculo perfecto de qué cuentas existen. Lo que hay que comparar entre las dos ramas no es solo el texto: es el código de estado, el destino de la redirección y el HTML | Al escribir un flujo con respuesta genérica, probar explícitamente la rama "no existe" y comparar la respuesta completa contra la rama "sí existe". Tratar cualquier 500 en un flujo de autenticación como una fuga de información, no solo como un bug |
| LL16 | Septiembre 2026 | Fase 2 | Seguridad (Auditoría) | Un `if` huérfano que quedó de una versión anterior hacía que cada rechazo de token escribiera **dos** líneas en el log de auditoría | Un log que cuenta doble es tan inútil como uno que omite: cualquier umbral, alerta o conteo que se construya sobre ese archivo queda inflado, y el error no se ve al leer una línea suelta — solo al contar | Las pruebas del log deben afirmar **cuántas** veces aparece cada evento, no solo que aparece. Ya se hacía así en `test_bruteforce.py`; se extendió al resto |
| LL17 | Septiembre 2026 | Fase 2 | Técnico (Refactor) | Al extraer la validación de contraseñas a una función compartida, el bloque original quedó en su lugar además de la llamada nueva. El código duplicado no produjo ningún error visible | El riesgo real de extraer una función no es escribirla mal, es **no borrar el original**. Queda código duplicado que no avisa de nada, y cuando después se corrige el bug suele corregirse en la copia equivocada, así que el arreglo "no funciona" sin explicación | Después de extraer una función, buscar el código original por una cadena literal suya (un mensaje, una constante) y confirmar que ya no existe más que dentro de la función nueva |
| LL18 | Septiembre 2026 | Fase 2 | Técnico (Python / Flask) | Al agregar la ruta de MFA se escribió un segundo `def dashboard()` en `auth.py` en lugar de mover el existente. La app dejó de arrancar con `AssertionError: View function mapping is overwriting an existing endpoint function: auth.dashboard` | En Python, dos funciones con el mismo nombre en un módulo **no son un error**: la segunda sobreescribe a la primera en silencio. Aquí falló solo porque Flask registra el endpoint por el nombre de la función y detectó el duplicado. Es la misma forma de LL17 —duplicar en vez de mover— pero esta vez el fallo fue **cerrado y ruidoso**, que es la única razón por la que se encontró en segundos en lugar de en producción | Al agregar una ruta, buscar el nombre de la función en el archivo antes de escribirla. Y valorar los frameworks que se niegan a arrancar ante una ambigüedad: un `AssertionError` al inicio vale más que un comportamiento raro después |
| LL19 | Septiembre 2026 | Fase 2 | Seguridad (Auditoría) | Al dar de alta el evento de activación de MFA se agregaron tres claves al catálogo —`mfa_on`, `mfa_activated` y `enabled_mfa`— que eran el mismo evento con tres nombres, y una de ellas con la descripción de un evento distinto | Un catálogo cerrado impide usar un evento **no registrado**, pero no impide **registrar el mismo evento tres veces**. La disciplina que falta no la puede dar el código: *un evento, un significado*. Con tres claves para lo mismo, ningún conteo posterior cuadra y el log queda inservible para exactamente aquello para lo que se construyó (LL16) | Antes de agregar un evento, preguntar si alguno de los existentes ya significa eso. Y al revés: cuando el éxito del segundo factor podía ser `mfa_success`, se dejó `login_success` porque en los dos caminos significa lo mismo —se creó una sesión— y separarlos obligaría a sumar dos claves para contar logins |
| LL20 | Septiembre 2026 | Fase 2 | Seguridad (Diseño) | Para darle a los códigos TOTP un contador de intentos propio sin tocar el esquema, se consideró prefijar la clave existente: `is_blocked(db, f"mfa:{username}")`. Se comprobó que `register()` solo valida que el usuario no esté vacío, así que **alguien puede registrarse literalmente como `mfa:ana`** y sus fallos de contraseña caerían en el contador de los códigos de `ana` | Un espacio de nombres por prefijo solo funciona si el separador **no puede aparecer en los datos**. Cuando el dato viene del usuario y no está restringido, el prefijo no separa nada: lo colisiona. Es inyección con otro disfraz — mezclar control y datos en una misma cadena | Antes de usar un prefijo como espacio de nombres, comprobar qué caracteres acepta realmente el campo de origen. Si el dato es libre, la separación va en una columna aparte, no en la cadena |
| LL21 | Septiembre 2026 | Fase 2 | Pruebas (Diseño) | `test_intento_bloqueado_no_prolonga_el_bloqueo` falló en una corrida completa y pasó sola inmediatamente después. Los timestamps del log mostraron un hueco de **cinco minutos y medio** a media corrida: la máquina se atascó, la ventana de 10 segundos expiró, el bloqueo se liberó y los intentos siguientes sí se registraron | Una ventana de prueba puede ser holgada frente al costo **conocido** de la operación (los ~330 ms de bcrypt, LL10) y seguir siendo frágil frente a algo que no se controla: el sistema operativo. El primer reflejo ante un fallo así es buscar el bug en el cambio recién hecho, y el cambio no tenía nada que ver | Ante el fallo de una prueba con ventana temporal: re-correrla aislada y mirar los timestamps **antes** de mirar el diff. Quedó anotado en `tests/README.md` para no volver a perseguirlo |
| LL22 | Septiembre 2026 | Fase 2 | Pruebas (Cobertura) | Al verificar las pruebas de MFA rompiendo cada control (LL14), quince de dieciséis mutaciones se pusieron en rojo. La que no: sustituir `hmac.compare_digest` por `==`, que pasó las 28 pruebas | Hay controles que **ninguna prueba razonable puede cubrir**. Medir una diferencia de microsegundos sobre seis dígitos dentro del mismo proceso no da señal estable, a diferencia de los ~330 ms de bcrypt que sí permiten medir un oráculo por latencia en 4.1. Lo importante es la diferencia entre *no tener cobertura* y *saber que no se tiene*: el primero es un punto ciego, el segundo es un riesgo gestionado | Cuando una mutación no se atrapa, no inventar una prueba frágil que dé falsa tranquilidad. Documentar el hueco en tres lugares —el comentario del código, `tests/README.md` y el Risk Register— para que quien toque ese control sepa que nada lo va a detener |
| LL23 | Septiembre 2026 | Fase 2 | Seguridad (Configuración) | Al abrir WBS 4.5.4 para acortar la vida de la sesión, se leyó el código de Flask instalado antes de tocar el valor. `PERMANENT_SESSION_LIFETIME` **no** era la vida máxima que el nombre sugiere y que la deuda daba por hecha: `SESSION_REFRESH_EACH_REQUEST` viene en `True` por default y vuelve a firmar la cookie en cada petición, así que la ventana se desliza. El riesgo residual de R9 estaba enunciado **de menos** —decía "hasta 14 días" cuando en realidad una cookie robada que se usa no caducaba nunca, porque el atacante la renovaba al usarla— | Un default razonable de un framework puede convertir un control en otro distinto sin que nada en la configuración propia lo delate. El nombre de una clave describe la intención de quien la diseñó, no lo que hace en combinación con el resto de los defaults. Y la lección de gestión: **un riesgo residual mal medido es peor que uno no documentado**, porque da por acotado algo que no lo está y nadie vuelve a mirarlo | Antes de ajustar el valor de una opción de seguridad, leer cómo la consume la librería —no solo su documentación— y buscar qué **otras** opciones la modifican. Al cerrar un paquete que atiende un riesgo residual, releer su enunciado y corregirlo si la implementación demostró que decía otra cosa |
| LL24 | Septiembre 2026 | Fase 2 | Gestión de proyecto | Durante tres sesiones se dio por pendiente registrar en el Charter que la suite de pruebas se había adelantado a 4.1. Al abrir el Charter en el cierre de fase, el **cambio #3 ya estaba escrito**, con su justificación completa. Lo que estaba desactualizado era la nota de trabajo que se venía arrastrando entre sesiones, no el artefacto | Una nota de trabajo que **copia** el estado de un artefacto formal se desincroniza en cuanto el artefacto cambia, y a partir de ahí se decide sobre la copia. El síntoma no es un error visible: es trabajo que se re-planea o se re-hace porque el resumen dice que falta. El artefacto es la fuente de verdad; la nota debe **apuntar** a él, no repetirlo | En las notas de traspaso, referenciar el artefacto ("ver Charter §10") en lugar de reproducir su contenido. Y al cerrar una fase, verificar los pendientes de gobernanza **abriendo el documento**, no leyendo el resumen |
| LL25 | Septiembre 2026 | Fase 2 | Pruebas (Diseño) | En las pruebas end-to-end de 4.6.1, el segundo paso del login con MFA falló. La causa no era un defecto: el guion hizo en dos segundos lo que una persona hace en veinte, así que reutilizó el mismo código TOTP con el que acababa de activar el enrolamiento — que queda consumido a propósito. Un segundo "fallo" de la misma corrida (un 405) tampoco era de la aplicación: la URL del guion estaba mal escrita | En una corrida automatizada, **un rechazo puede ser el control funcionando**. Un guion no reproduce los tiempos humanos, y varios controles de seguridad dependen justamente del tiempo: ventanas TOTP, expiración de tokens, contadores deslizantes. Es el reverso de LL10 —allá una ventana demasiado corta hacía que la prueba midiera otra cosa; aquí una ejecución demasiado rápida dispara un control legítimo— y en ambos casos el error es leer el resultado sin mirar el reloj | Ante un fallo en una prueba end-to-end, comprobar primero si algún control **debía** rechazar eso, antes de tocar la aplicación. En los guiones que atraviesen un control temporal, esperar explícitamente al siguiente periodo en vez de confiar en que el tiempo pase solo |

---

*Próxima entrada esperada: cierre de Fase 0 (ver WBS, tarea 2.7.3).*
